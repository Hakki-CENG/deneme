{
  "name": "@haf/control-api",
  "version": "1.65.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build -w @haf/engine && npm run build -w @haf/session-worker && npm run build -w @haf/canvas-web && tsx watch src/main.ts",
    "start": "node dist/main.js",
    "build": "tsc -b tsconfig.json --force",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cookie": "^10.0.1",
    "@fastify/cors": "^10.0.2",
    "@fastify/formbody": "^9.0.0",
    "@fastify/static": "^10.1.3",
    "@haf/engine": "1.65.0",
    "fastify": "^5.2.1",
    "fastify-raw-body": "^5.0.0",
    "jose": "^6.2.9",
    "zod": "^3.24.2"
  }
}
